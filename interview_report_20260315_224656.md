# 模拟面试报告

**项目**：Modular RAG MCP Server  
**面试时间**：2026-03-15 22:46:56  
**面试风格**：DEEP（深挖发散型）  
**掷骰结果**：4  
**评分**：3/10

---

## 一、面试记录（原文）

| 题号 | 问题（原文） | 候选人回答（原文，不摘要） |
|-----|-------------|---------------------------|
| Q1 | 这个系统里有哪几类存储？它们各自负责什么？为什么不能只用一个？ | 三类分别使向量存储chmora储存向量以及元数据，bm25以及图片二进制文件由文件存储一个文件一个集合，文件完整性id以及哈希值和图片元数据使用的sqlite |
| Q2 | 这个系统里有哪几类存储？它们各自负责什么？为什么不能只用一个？ | 重新回答刚刚的问题，三类分别使向量存储chmora储存向量以及元数据，bm25以及图片二进制文件由文件存储一个文件一个集合，文件完整性id以及哈希值和图片元数据使用的sqlite。避免数据混乱，特定数据使用特定方式库，同样有文档管理器来协调同时删除加载 |
| Q3 | 如果现在删除一个文档，四路存储（`Chroma`、`BM25`、图片存储、`ingestion_history.db`）的删除顺序你会怎么设计？其中一路删除失败时，你会怎么保证系统最终一致性？ | 删掉了 Chroma 的不会再次执行删除操作因为实现了幂等性 |
| Q4 | 你会怎么定义各存储的“已删除即成功”语义？请你分别说下这 4 路在二次重试时的返回约定，例如：`Chroma`：`deleted_count=0` 算成功吗？`BM25`：文档不存在时返回什么状态？图片：文件不存在是 warning 还是 success？`ingestion_history.db`：`remove_record=0` 该怎么判定？你按“状态码 + 是否可重试”给我一个统一表述。 | Chroma：deleted_count=0 算成功，BM25success图片successingestion_history.dbsuccess |
| Q5 | 你简历里写了“集成高德地图 MCP 工具链，完成工具接入、异步转同步包装、工具缓存和 mock fallback 设计”。请你具体讲讲：这个 `mock fallback` 是怎么触发的？触发后返回结构长什么样？你是怎么保证上游 `LangGraph` 节点不因为真实工具失败而中断的？ | 不会 |
| Q6 | 如果让你现在补上这个能力，你会如何设计一个最小可用 `fallback contract`？请至少说清 3 点：1) 触发条件（超时/429/5xx/解析失败等）2) 返回字段（例如 `status`、`source`、`data`、`error_code`）3) `LangGraph` 节点如何根据 `status` 决定继续、重试还是降级。 | 不会 |
| Q7 | 如果要把这个系统部署给多个团队使用（多租户），现有架构你会改哪些地方？你认为最难的点是什么？ | 引入记忆模块实现通话id唯一 |
| Q8 | 你会把 `tenant_id` 放在哪一层做强约束：请求入口鉴权层、检索参数层，还是存储主键层？请给一个你能落地的键设计，比如 `collection = {tenant_id}_{biz_space}` 或 metadata 强制带 `tenant_id`，并说明怎么防止跨租户误召回。 | 不会 |

---

## 二、参考答案（本场涉及）

### <a id="a-document-manager"></a>Q: 删除一个文档需要操作哪几个存储？失败怎么办？

必须**协调删除四路存储**：
1. **Chroma 向量库**：按 `metadata.source` 删除所有 Chunk 向量
2. **BM25 倒排索引**（`data/db/bm25/`）：移除所有词条的倒排条目
3. **ImageStorage**（`data/images/`）：删除关联图片文件
4. **FileIntegrity**（`ingestion_history.db`）：删除 SHA256 处理记录，使文件可重新摄取

**为什么必须四路同步**：只删 Chroma 不删 BM25 → 下次 Hybrid Search 从 BM25 召回已不存在的 Chunk，数据不一致；不删 FileIntegrity → 重传同一文件被认为"已处理"而跳过。

**失败策略**：尽力删除（best-effort），各存储独立尝试，失败记录错误日志但不阻塞其他存储。生产级可引入两阶段提交。

---

## 三、简历包装点评

### 包装合理 ✅
- **“构建可溯源与可观测闭环”**：能够提到 Chroma / BM25 / SQLite / 图片文件四类存储分工与文档管理器协调删除，说明对系统分层有基础认知。

### 露馅点 ❌
- **“集成高德地图 MCP 工具链…mock fallback 设计”** → 无法说明触发条件、返回结构、上游容错机制（连续两轮回答“不会”）。**严重性：高**（该条是简历中的核心实现描述）。
- **“多租户部署可扩展”相关能力** → 无法落地 `tenant_id` 隔离策略与防串租户误召回方案。**严重性：中**。
- **“删除失败后重试与幂等”** → 仅能描述 Chroma 侧，不足以覆盖跨存储一致性与统一状态码设计。**严重性：中**。

### 改进建议
- 把“删除链路”背成固定模板：四路存储、每路成功语义、重试幂等、失败分类（可重试/不可重试）。
- 准备一版 `fallback contract` 固定回答：触发条件、统一返回字段、LangGraph 路由策略（继续/重试/降级）。
- 准备多租户标准答案：请求鉴权层 + 检索过滤层 + 存储键层三层隔离，并给出一套键设计与查询过滤示例。

---

## 四、综合评价

**优势**：
- 能给出系统存储分层的主干概念；对“为何不能只用一个存储”有正确方向。

**薄弱点**：
- 面向实现的关键细节准备不足，尤其是 fallback 机制与多租户隔离。
- 追问后回答快速失稳，缺少“状态机/协议/错误码”级别的工程化表达。

**面试官建议**：
- 针对简历每个强动词（“设计/主导/实现”）准备 1 套可复述的“触发条件-流程-数据结构-失败处理-验证方式”。
- 针对本项目至少背熟三块：Hybrid Search+RRF、四路删除幂等、MCP tool 输入输出契约。

---

## 五、评分

| 维度 | 分数（满分 10）| 评分依据 |
|-----|--------------|--------------------------------|
| 项目架构掌握 | 5 | 了解存储分层主干，但无法延展到跨存储一致性细节 |
| 简历真实性 | 2 | 简历中 fallback 与工具链设计无法展开，出现连续“不会” |
| 算法理论深度 | 2 | 本场未展示算法层计算与理论支撑 |
| 实现细节掌握 | 2 | 状态码、幂等重试、多租户隔离均未形成可落地方案 |
| 表达清晰度 | 4 | 有主干结论，但细节回答较短且缺少结构化表达 |
| **综合** | **3** | 具备入门框架认知，但与“项目主导/设计”要求存在明显差距 |

---

## 六、本场实际考察题目列表

1. 存储分层与职责边界
2. 四路删除顺序、失败恢复与最终一致性
3. 二次重试幂等的统一状态语义
4. MCP 工具链 `mock fallback` 触发与返回契约
5. `LangGraph` 节点容错路由策略
6. 多租户改造与 `tenant_id` 强约束设计
