# 模拟面试报告

**项目**：Modular RAG MCP Server  
**面试时间**：2026-03-19 22:21:05  
**面试官风格**：FAST（速攻广度型）  
**掷骰结果**：4  
**综合评分**：5.8/10

---

## 一、面试记录（原文逐字）

| 题号 | 问题（原文） | 候选人回答（原文，不摘要） |
|------|--------------|-----------------------------|
| Q1 | 在你这个系统里，涉及 Chroma、BM25、SQLite（如历史记录/图片索引）等多种存储。请你快速说明：每一类存储分别负责什么，为什么不能只用一个存储统一解决？ | chorma负责储存向量，BM25储存在集合文件中，sqlite分别储存文档储存记录以及图片元数据。一个储存会导致数据污染，以及chroma无法储存nm25关键字信息。我在项目中使用一个文档管理器可以协调这个储存一致 |
| Q2 | 你简历里写到“设计并实现了结合地图服务与 LLM 的智能旅行规划后端”，并且“基于 LangGraph 构建多 Agent 工作流”。如果按你说是你主导设计的，请具体讲一下：当时工作流里的错误处理和兜底策略是怎么迭代出来的，经历了几版，最终定稿依据是什么？ | 错误处理当时包括地图工具未加载。mcp连接错误超时等情况我当时想的就是通过降级使用模拟工具进行降级处理。以及如何在节点路径中出现错误会通过条件边导向错误函数使用固定计划返回。最终定稿依旧可以在大部分情况下能稳定返回结果 |
| Q3 | 如果这套系统要支持多租户（给多个团队同时使用），你认为现有架构需要改哪些地方？你觉得最难的点是什么？ | 入口检测层，查询层，储存层。需要给每个用户绑定一个单独id保证在使用中内容不会出现混乱，最难的应该就是每次验证id怎样保证id独特性不会被其他人使用污染 |

---

## 二、考察题目列表（本场）

1. 方向1（项目综述）：多存储分工与一致性（Chroma/BM25/SQLite）
2. 方向2（简历深挖）：多 Agent 工作流错误处理与兜底方案迭代
3. 方向3（技术深挖）：多租户改造点与核心难点

---

## 三、参考答案（针对本场题目）

### <a id="a-q1-storage"></a>Q1 参考答案：多存储分工与不可合并原因
- Chroma：存 Dense 向量和 metadata，用于语义检索。
- BM25 索引：存倒排结构和词项统计，用于关键词检索。
- SQLite：存流程性元数据（如 ingestion_history、image_index 等）。
- 不能合并为一个存储的原因：检索范式不同（向量 ANN vs 倒排）、数据结构不同、更新与删除一致性要求不同；强行单库会导致检索能力退化或工程复杂度上升。

### <a id="a-q2-fallback"></a>Q2 参考答案：工作流错误处理与兜底迭代
- 常见失败点：工具加载失败、MCP 超时、下游节点返回空结果/结构错误。
- 推荐迭代路径：
  1) 节点内 try/except + 明确错误码；
  2) 条件边统一导向 error-handler 节点；
  3) 引入降级策略（mock/fallback tool、固定骨架行程）；
  4) 通过回放日志和失败样本统计验证成功率、稳定性和超时率。
- 定稿依据应包含可量化指标，如成功返回率、超时率、平均耗时和兜底触发占比。

### <a id="a-q3-multitenant"></a>Q3 参考答案：多租户改造与难点
- 改造点：
  - 接入层：鉴权与租户识别（tenant_id/user_id）；
  - 查询层：全链路携带租户上下文，强制租户过滤；
  - 存储层：按 tenant 分库/分 collection/分 namespace；
  - 配置与密钥：租户级配额、限流、审计与隔离策略。
- 最难点：隔离正确性与一致性（防止越权查询/写入）、缓存与索引层的租户污染、以及高并发下的隔离性能平衡。

---

## 四、简历包装点评

### 包装合理 ✅
- “多存储协调一致”：回答提到了 Chroma、BM25、SQLite 的职责分工，并说明了单存储风险，方向正确。
- “错误处理与降级”：回答能给出 MCP 超时、工具未加载、条件边导向错误节点、mock fallback，属于真实工程中常见模式。
- “多租户基础意识”：回答提到入口/查询/存储三层与用户 ID 隔离，具备基本架构意识。

### 露馅点 ❌
- “主导设计并实现”这一强动词支撑不足：未给出版本迭代次数、关键决策点和量化定稿指标。**严重性：中**（影响可信度但非完全错误）。
- 多租户回答停留在原则层，缺少具体实现（例如租户级 collection 命名规则、鉴权 token 绑定策略、缓存隔离方案）。**严重性：中**。
- 术语与细节准确性存在小问题（如 Chroma/BM25 表述与拼写），反映对底层实现细节掌握还不稳。**严重性：低**。

### 改进建议
- 针对“主导设计”类描述，准备一套固定叙述：失败案例 → 迭代版本 → 指标变化（成功率、延迟、错误率）。
- 多租户准备可落地方案：租户鉴权、存储分区、查询过滤、缓存 key 设计、审计日志。
- 统一关键术语并补齐可背诵细节（如索引结构、超时阈值、回退触发条件）。

---

## 五、综合评价

**优势**：
- 具备系统分层意识，能快速指出入口层、查询层、存储层和隔离目标。
- 对降级与兜底策略有实战直觉，能说出 mock fallback 与错误路径分流。

**薄弱点**：
- 量化和复盘维度不足，回答缺少“如何验证方案有效”的指标化证据。
- 关键实现细节不够具体，难以支撑“主导设计”强表述。

**面试官建议**：
- 补齐“指标-决策-结果”闭环表达；
- 为每个核心模块准备 2-3 个代码级细节（数据结构、关键参数、失败处理路径）；
- 下一轮建议切换 `CODE` 风格做源码拷问训练。

---

## 六、评分

| 维度 | 分数（满分 10） | 评分依据 |
|------|------------------|----------|
| 项目架构掌握 | 6.5 | 能说出分层与多存储职责，但细节边界（索引结构/一致性机制）不够完整 |
| 简历真实性 | 5.5 | 方向正确但“主导设计”的迭代证据和量化支撑不足 |
| 算法理论深度 | 5.0 | 本场未体现公式/算法参数级别阐述，深度一般 |
| 实现细节掌握 | 5.5 | 提到错误分流和降级，但缺少具体实现点与参数细节 |
| 表达清晰度 | 6.5 | 逻辑基本连贯，表达可理解，术语精度仍可提升 |
| **综合** | **5.8** | 满足基础岗位沟通与理解，距离高可信“主导级”仍有差距 |
