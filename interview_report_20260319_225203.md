# 模拟面试报告（第二轮）

**项目**：Modular RAG MCP Server  
**面试时间**：2026-03-19 22:52:03  
**面试官风格**：MIX（随机混搭型）  
**掷骰结果**：4  
**问答条数**：6  
**综合评分**：6.1/10

---

## 一、面试记录（原文逐字）

| 题号 | 问题（原文） | 候选人回答（原文，不摘要） |
|------|--------------|-----------------------------|
| Q1 | 如果项目中某个 LLM Provider 挂了，系统会怎么表现？你们有怎样的降级机制来保证服务可用？ | 如果挂了是指key不可用会在初始化提示错误，不过我们可以实现降级机制通过提供其他provider来实现降级，如果是连接超时或者其他错误在全链路中都提供了降级策略提供硬编码方式rule规则实现降级 |
| Q2 | 你刚才提到“初始化提示错误 + 其他 provider 降级 + 全链路 rule 兜底”。我追问一个细节：你们是在哪一层决定 fallback 顺序的？触发降级的判定条件具体有哪些（比如超时阈值、异常类型、重试次数），以及怎么避免“静默失败”（表面成功但结果质量明显下降）？ | 在链路层决定fallback顺序d，降级判定包括超时，连接失败，在embeding类似bitch批次处理中会在不同批次进行连接，不同条件触发会在trace中记录在白盒化显示中可以查询是什么地方出现错误 |
| Q3 | 你简历写了“掌握 Agent 开发、工具调用全过程”，那请你按代码级别回答：如果在这个项目里新增一个 Embedding Provider，你要改哪 3 个具体文件（给到路径级别），分别改什么内容（类/工厂映射/配置项名）？ | 在libsd embeding中会新增一个embed ing文件来实现它的连接方式使用基本embeding类。然后改变工厂函数进行注册，最后可以在setting文件中修改 |
| Q4 | 你这个回答还是偏抽象，我需要更可验证的细节。Q4（HARD，方向2：简历深挖）：你简历写的是“基于 LangGraph 构建多 Agent 工作流，并加入错误处理与兜底方案”。这套兜底规则到底是不是你亲自写的？具体迭代了几版、每版改了什么、哪条失败样例逼着你改的？如果你说不出具体 case，我会倾向判断这部分是包装。 | 是我写的，迭代了三次，mcp连接超时等会导致工具不可用所以在工具不可用的时候我们实现了模拟工具提供信息来供agent使用。在后面链路搭建中我们发现有时候agent会出现错误导致链路崩塌使用我们设置了条件边来导向错误函数生成一份写好的旅行计划。第三次我们使用mcp连接我们的rag系统如果超时会转向stdio直连如果还超时会返回空但是不影响最终结果 |
| Q5 | 你刚才提到了“三次迭代 + 工具 mock + 条件边兜底 + 双重超时回退”，信息量不错。Q5（方向3：技术深挖）：回到检索核心，RRF 融合里 k 参数你们怎么定的？如果把 k 从 60 改成 10，排序结果通常会出现什么变化？ | 提供论文擦查询得到的经验值。如果改为10会导致排序结果更趋向于每个链路得到的结果都靠前的文档块 |
| Q6 | 你说到“论文经验值 + k=10 更强调前排文档”，这个判断基本正确。Q6（DEEP 追问）：那在你的实现里，k 是写死常量还是可配置项？你们有没有做过 A/B（例如 k=10/30/60）并用 Hit Rate@K 或 MRR 做对比，最后为什么保留当前值？ | k是写死常量，目前还没有做过不同条件下的A/B测试 |

---

## 二、参考答案（本场问到的能力点）

### <a id="a-provider-fallback"></a>1) Provider 故障降级机制
- 推荐做法：初始化阶段做 key/endpoint 健康检查；运行时按异常类型（超时/限流/鉴权/连接失败）触发分级 fallback。
- fallback 顺序应在统一编排层配置化管理（primary → secondary → mock/rule-based）。
- 必须记录触发原因、重试次数、最终命中 provider，避免“看似成功但不可观测”。

### <a id="a-embedding-provider-3files"></a>2) 新增 Embedding Provider 三步
- 新增实现文件：`src/libs/embedding/<provider>.py`，继承 `BaseEmbedding` 并实现接口方法。
- 注册工厂映射：`src/libs/embedding/factory.py` 新增 provider 名称到类的映射。
- 更新配置：`config/settings.yaml` 中切换 `embedding.provider` 及其参数。

### <a id="a-rrf-k"></a>3) RRF 中 k 的作用
- 公式：$Score = \frac{1}{k + rank_d} + \frac{1}{k + rank_s}$。
- k 越小，头部排名差异被放大；k 越大，融合分数更平滑。
- 实践中常从 60 起步，最终应通过离线评测（Hit Rate@K / MRR）验证。

---

## 三、简历包装点评

### 包装合理 ✅
- 能说出“超时/连接失败→降级”“Trace 记录触发条件”“条件边导向错误函数”等真实工程关键词。
- 能给出新增 Provider 的基本流程（实现类→工厂注册→配置切换），方向正确。
- 对 RRF 的 k 参数影响有基本直觉（k=10 更强调前排）。

### 露馅点 ❌
- 代码级细节不足：在“给出 3 个具体文件路径”问题上未准确给出可核验路径与配置键名。**严重性：中**。
- 可量化验证不足：承认 k 为写死且未做 A/B，说明“参数策略”更多是经验继承，缺乏实验闭环。**严重性：中**。
- 回答中有术语/拼写混乱（如 embedding/batch 等），会影响面试官对实现熟练度判断。**严重性：低**。

### 改进建议
- 准备“代码级速答卡”：每个核心模块至少 3 个文件路径 + 关键类名 + 配置键名。
- 补一版最小 A/B 结果（k=10/30/60），哪怕是小样本，也能显著提升可信度。
- 降级策略准备固定模板：触发条件、优先级、重试策略、观测指标、用户侧可见行为。

---

## 四、综合评价

**优势**
- 工程意识较强：降级、兜底、可观测这些“生产问题”意识到位。
- 架构思维清楚：知道在链路层统一处理 fallback，能描述整体思路。

**薄弱点**
- 代码级精确度仍不足，面对路径级追问时回答不够“可核验”。
- 评估驱动能力弱，参数选择尚未形成“实验—结论”闭环。

**面试官建议**
- 下一轮优先做 CODE 模式专项：每题都按“文件→函数→参数→失败处理”四段式回答。
- 补一页“关键参数实验表”（RRF k、top_k、rerank on/off）并背熟结论。

---

## 五、评分

| 维度 | 分数（满分 10） | 评分依据 |
|------|------------------|----------|
| 项目架构掌握 | 6.8 | 能说明链路层 fallback 与全链路兜底，但细节不够落地 |
| 简历真实性 | 6.2 | 三次迭代回答较可信，但缺少可复现 case 编号与日志证据 |
| 算法理论深度 | 6.0 | RRF 参数影响答对主干，实验验证不足 |
| 实现细节掌握 | 5.6 | Provider 新增流程正确，但路径级回答不够精确 |
| 表达清晰度 | 6.0 | 逻辑基本完整，术语精度和表述规范性可提升 |
| **综合** | **6.1** | 基础扎实、工程意识不错，距“高可信源码级回答”还有距离 |
